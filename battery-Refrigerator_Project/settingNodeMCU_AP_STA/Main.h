#include "Setup.h"

extern const int Max_Reading_Buffer;
extern const int delay_per_loop;

boolean toggle_notifier_pin;
// I may define a counter (not a delay) and a counter_max to toggle at the right time.
